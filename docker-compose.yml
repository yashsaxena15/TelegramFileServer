services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "9000:8000"
    depends_on:
      - mongo

volumes:
  mongo_data:
